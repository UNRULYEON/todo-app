{
  "name": "@todo-app/server",
  "version": "0.0.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only --exit-child index.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@todo-app/trpc": "workspace:^",
    "@trpc/client": "^10.29.1",
    "@trpc/react-query": "^10.29.1",
    "@trpc/server": "^10.29.1",
    "express": "^4.18.2",
    "react-query": "^3.39.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2"
  }
}
